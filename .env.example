# URL of your deployed Cloudflare Worker (see worker/ and README).
# This value is public (it ends up in the site's JS) — that's fine.
# Example:
# PUBLIC_API_URL=https://javaded-api.<your-subdomain>.workers.dev
